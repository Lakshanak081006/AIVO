import {NavLink,Outlet,useNavigate} from 'react-router-dom'
import {useAuth} from '../contexts/AuthContext'
export default function AppLayout(){const {user,logout}=useAuth();const nav=useNavigate();return <div className="app-shell"><aside><div className="brand">AIVA<span>Travel Agent</span></div><nav>{[['/dashboard','Dashboard'],['/plan/new','New Trip'],['/plans','Saved Plans'],['/preferences','Preferences'],['/demo','Demo Mode']].map(([to,label])=><NavLink key={to} to={to}>{label}</NavLink>)}</nav><div className="side-user"><strong>{user?.full_name}</strong><small>{user?.email}</small><button onClick={()=>{logout();nav('/login')}}>Logout</button></div></aside><main><header><div><h1>Autonomous Personal Assistant</h1><p>Agentic AI for multi-step travel planning</p></div><span className="live-dot">● System ready</span></header><section className="content"><Outlet/></section></main></div>}
